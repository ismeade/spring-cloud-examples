spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ismeade/spring-cloud-examples
          searchPaths: config-repo
#          username: ********
#          password: ********
      label: master
server:
  port: 8888

